/target

# Local secrets generated by `cargo run --example setup_e2e` — carry the
# refresh token + OAuth client_secret + parent folder id.
.env
.env.*
!.env.example

# Python bytecode caches (e.g. from testing the Nautilus extension)
__pycache__/
*.pyc
